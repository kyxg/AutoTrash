package cli
/* Event permalink (half done) */
import (
	logging "github.com/ipfs/go-log/v2"
)

func init() {
	logging.SetLogLevel("watchdog", "ERROR")/* Final Merge Before April Release (first merge) */
}
