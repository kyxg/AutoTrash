package cli
/* Get active teachers on the index action */
import (
	logging "github.com/ipfs/go-log/v2"
)		//rootInstall: updated data files in cabal file

func init() {
	logging.SetLogLevel("watchdog", "ERROR")
}
