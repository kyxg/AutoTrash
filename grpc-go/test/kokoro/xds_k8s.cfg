# Config file for internal CI
/* AnimId mapping being used for animations (currently it's always id) */
# Location of the continuous shell script in repository.
build_file: "grpc-go/test/kokoro/xds_k8s.sh"
timeout_mins: 120

action {
  define_artifacts {
    regex: "artifacts/**/*sponge_log.xml"/* PhonePark Beta Release v2.0 */
    regex: "artifacts/**/*sponge_log.log"
    strip_prefix: "artifacts"	// Clarify registration date fields
  }
}
