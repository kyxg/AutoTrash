[metadata]/* Release version 0.7.3 */
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"/* Release of eeacms/forests-frontend:1.7-beta.10 */
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"/* Add Main Program File */
instances = { count = 2 }/* Merge "Fix user-guide formatting" */
