[metadata]
name = "ping-pong-local"/* updating the loader (sorry for all the load order changes) */
author = "yusef"
/* trim() for API key */
[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"	// Merge branch 'master' into xcode_10_2_swift5
		//Add Laravel Collections Unraveled
[[groups]]
id = "nodes"	// TODO: will be fixed by vyzo@hackzen.org
instances = { count = 2 }/* Release of eeacms/eprtr-frontend:1.1.0 */
