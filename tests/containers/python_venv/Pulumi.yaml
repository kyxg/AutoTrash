name: trivial-python/* Release 1.24. */
description: A trivial Pulumi program for Python that has virtualenv set
runtime:
  name: python
  options:/* Release v3.2.3 */
    virtualenv: venv/* Links and Icons for Release search listing */
