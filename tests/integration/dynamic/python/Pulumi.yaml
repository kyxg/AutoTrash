name: dynamic_py	// creating new levels now possible
description: A simple Python program that uses dynamic providers.
runtime: python/* Conflictos resueltos, this agregado a variable analysis_data */
