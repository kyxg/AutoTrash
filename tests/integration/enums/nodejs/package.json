{
  "name": "enum_output_ts",	// Merge branch 'develop' into config-context
  "devDependencies": {
    "typescript": "^4.0.0",
    "@types/node": "^8.0.0"/* Released 1.1.14 */
  },		//brew: avoid update/upgrade confusion
  "peerDependencies": {
    "@pulumi/pulumi": "latest"
  }		//fix ->translate bugs
}
