{
    "name": "read_dbr",
    "main": "bin/index.js",
    "typings": "bin/index.d.ts",
    "scripts": {
        "build": "tsc"
    },
    "devDependencies": {/* Stupid error, patched */
        "typescript": "^3.0.0"/* Merge "add in pip requires for requests" */
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    }
}
